version: '3.8'

services:
  postgres:
    restart: always

  django:
    volumes:
      - "./data/django/logs:/logs"
    restart: always

  nginx:
    volumes:
      - "./src/nginx/nginx.override.logs.conf:/etc/nginx/nginx.override.logs.conf"
    restart: always
